<!DOCTYPE html>
<html lang="cs">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}CodeHub{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

        {% block stylesheets %}
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
            <link rel="stylesheet" href="{{ asset('css/reset.css') }}">
            <link rel="stylesheet" href="{{ asset('css/header.css') }}">
            <link rel="stylesheet" href="{{ asset('css/hero.css') }}">
            <link rel="stylesheet" href="{{ asset('css/card-block.css') }}">
            <link rel="stylesheet" href="{{ asset('css/accordion.css') }}">
        {% endblock %}
    </head>
    <body>
        <!-- Header komponenta - implementujte HTML strukturu a stylování podle Figma designu -->
        <nav class="header-data">
            <div class="header-data-wrapper container">
            <div class="header-data-mobile">
                    <img src="{{ asset('images/logo.svg') }}" alt="Logo" class="logo-url">
                    <img src="{{ asset('icons/hamburger.png') }}" alt="Hamburger menu" class="hamburger-menu">
            </div>

            <div class="menu-items">
                {% for item in menu_items %}
                    <a href="{{ item.url }}" class="menu-item">{{ item.name }}</a>
                {% endfor %}
            </div>
            </div>
        </nav>

        {% block body %}{% endblock %}

        {% block javascripts %}
            <script src="{{ asset('js/main.js') }}"></script>
        {% endblock %}
    </body>
</html>
